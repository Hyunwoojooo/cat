import 'package:flutter/material.dart';

// 치즈고양이 컨셉 색상 팔레트
const P_1 = Color(0xFFFFB74D); // 주황색 (치즈 색상)

const P_2 = Color(0xFFFFCC80); // 밝은 주황색

const P_3 = Color(0xFFFFF8F0); // 연한 크림색

const B_1 = Color(0xFF1C1D1F); // 검정색 (유지)

const B_2 = Color(0xFF46484D); // 진한 회색 (유지)

const B_3 = Color(0xFF83868D); // 중간 회색 (유지)

const B_4 = Color(0xFFCED0D5); // 연한 회색 (유지)

const B_5 = Color(0xFFF0F1F5); // 매우 연한 회색 (유지)

const MIXIN_CLICK = Color(0xFFFFE0B2); // 클릭 시 밝은 주황색

const WHITE = Color(0xFFFFFFFF); // 흰색 (유지)

const BROWN = Color(0xFF8D6E63); // 갈색 (유지)

const BROWN_LIGHT = Color(0xFFFFF8E1); // 연한 갈색 (유지)

const MIXIN_47 = Color(0xFF474747); // 회색 (유지)

const ErrorColor = Color(0xFFD84137); // 에러 색상 (유지)
